export const metadata = {
  title: 'Supply Chain | Accra Threads Admin',
};

export default function SupplyChainLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
